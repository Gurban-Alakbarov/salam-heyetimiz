meta {
  name: Open-command history (all users)
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/admin/v1/devices/:deviceId/commands
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminJwt}}
}
