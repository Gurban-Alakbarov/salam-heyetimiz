targets:
  $default:
    builders:
      json_serializable:
        options:
          # Dart camelCase fields <-> JSON snake_case keys (backend contract).
          field_rename: snake
          explicit_to_json: true
          create_to_json: true
