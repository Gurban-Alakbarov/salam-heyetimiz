meta {
  name: Remove a resident from a device roster
  type: http
  seq: 18
}

delete {
  url: {{baseUrl}}/admin/v1/devices/:deviceId/users/:userId
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminJwt}}
}
