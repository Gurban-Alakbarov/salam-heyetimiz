<?xml version="1.0" encoding="utf-8"?>
<!-- Pending background for the door widget — a distinct amber (#E38A00) so an open in
     progress is clearly different from the normal green state ("operation running, don't
     tap again"). Same rounded shape as door_widget_bg. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="#E38A00" />
    <corners android:radius="16dp" />
</shape>
