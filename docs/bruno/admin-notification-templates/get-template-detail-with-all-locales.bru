meta {
  name: Template detail with all locales
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/admin/v1/notification-templates/:key
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminJwt}}
}
