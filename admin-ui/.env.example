# Base URL for the admin API. Leave EMPTY to use:
#   - dev:  the Vite proxy (/admin/v1 -> https://admin.salamheyetimiz.com)
#   - prod: same-origin (SPA served from https://admin.salamheyetimiz.com)
# Set to an absolute origin (e.g. https://admin.salamheyetimiz.com) only when hosting the SPA elsewhere.
VITE_API_BASE_URL=
