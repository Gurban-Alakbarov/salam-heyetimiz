meta {
  name: Mark biometric unlock as enrolled for this install
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/v1/me/biometrics/enroll
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt}}
}
