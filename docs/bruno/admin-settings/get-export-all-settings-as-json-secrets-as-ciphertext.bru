meta {
  name: Export all settings as JSON (secrets as ciphertext)
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/admin/v1/settings/export
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminJwt}}
}
